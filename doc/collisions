Collisions are based on Map Chunks

collision check {
	check current chunk you're on
	check x adjacent chunks outward
		where x is ((Collision Area / Chunk Area) + 1)
		where adjacent means +x, +y, +xy (make sure diagonals)
	
	+1 comes from if you're in between chunks
}

